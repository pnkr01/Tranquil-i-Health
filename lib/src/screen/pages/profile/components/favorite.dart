import 'package:flutter/material.dart';

class FavViewScreen extends StatelessWidget {
  const FavViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
